  <!-- header End Here -->
    <div class="c-hero c-hero--page">
        <div class="c-hero__background o-trees-bg">
            <img class="lazy entered loaded" src="{{ $bannerImage }}" title="{{ $name }}" alt="{{ $name }}">    
        </div>
        <div class="guides" data-aos="fade-up-sm" data-aos-duration="800">
            <h1 class="c-hero__title">{{$name}}</h1>
        </div>
    </div>